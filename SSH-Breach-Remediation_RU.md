# Руководство по устранению последствий взлома SSH

## 🚨 Обзор инцидента

Этот документ описывает реальный инцидент взлома SSH на сервере Debian 10 и предоставляет пошаговые команды для устранения последствий.

### Краткое описание атаки
- **Цель**: Сервер Debian 10 с пользователем `kodi`
- **Точка входа**: Роутер MikroTik с правилом dst-nat, открывшим SSH порт 2223 в интернет
- **Метод атаки**: Автоматический перебор паролей ботнетом
- **Вредонос**: Криптомайнер, замаскированный под `kauditd0` (имя легитимного процесса ядра)
- **Источники атаки**: Множественные IP из Нидерландов, Германии, Китая, России
- **Механизмы постоянства**: Cron задачи, SSH authorized_keys, скрипты автозапуска

### Начальные симптомы
```bash
# Подозрительный процесс, потребляющий 400-546% CPU
kodi      3800  546  8.2 2433864 2018232 ?     Ssl  14:14   0:54 kauditd0
```

**Красные флаги**:
- `kauditd0` запущен от пользователя `kodi` (должен быть потоком ядра)
- Экстремальное использование CPU (546% = ~5.5 ядер процессора)
- Процессы `edac0` также запущены под пользователем `kodi`

---

## 🔍 Первоначальное расследование

### 1. Проверка подозрительных процессов
```bash
# Просмотр всех процессов пользователя kodi
sudo ps aux | grep kodi

# Проверка использования CPU
top -bn1 | head -20
```

### 2. Проверка истории SSH входов
```bash
# Проверить успешные входы
sudo grep "Accepted password" /var/log/auth.log | grep kodi | tail -30

# Проверить неудачные попытки входа
sudo grep "Failed password" /var/log/auth.log | grep kodi | tail -20

# История входов
sudo last -20
```

### 3. Идентификация вредоносных файлов
```bash
# Найти недавно измененные файлы в домашней директории kodi
sudo find /home/kodi -type f -mtime -7 -ls

# Проверить скрытые директории
sudo ls -laR /home/kodi/
```

**Обнаружено местоположение вредоноса**: `/home/kodi/.configrc7/`

### 4. Изучение компонентов вредоноса
```bash
# Просмотр структуры директории
sudo ls -la /home/kodi/.configrc7/

# Проверить SSH ключи, добавленные атакующими
sudo cat /home/kodi/.ssh/authorized_keys

# Проверить механизм постоянства через cron
sudo cat /home/kodi/.configrc7/cron.d
sudo crontab -u kodi -l
```

---

## 🛑 Немедленные действия в ответ на атаку

### 1. Остановка SSH сервиса (экстренно)
```bash
# Немедленно остановить SSH
sudo systemctl stop sshd

# ИЛИ заблокировать SSH порт через iptables
sudo iptables -A INPUT -p tcp --dport 22 -j DROP
sudo iptables-save
```

### 2. Уничтожение вредоносных процессов
```bash
# Убить конкретный вредоносный процесс
sudo kill -9 3800  # PID фейкового kauditd0

# Убить все процессы пользователя kodi
sudo pkill -9 -u kodi

# Убить по имени процесса
sudo killall -9 kauditd0
sudo killall -9 edac0
sudo killall -9 kswapd00
```

### 3. Удаление вредоносных файлов
```bash
# Удалить директорию с вредоносом
sudo rm -rf /home/kodi/.configrc7/

# Удалить SSH ключи атакующих
sudo rm -f /home/kodi/.ssh/authorized_keys
sudo rm -rf /home/kodi/.ssh/

# Удалить SSL сертификаты, используемые вредоносом
sudo rm -f /home/kodi/cert*.pem
```

### 4. Удаление механизмов постоянства
```bash
# Удалить cron задачи
sudo crontab -r -u kodi

# Проверить и удалить из системного cron
sudo cat /etc/crontab
sudo ls -la /etc/cron.d/
sudo ls -la /etc/cron.hourly/
sudo ls -la /etc/cron.daily/

# Проверить systemd сервисы
sudo systemctl list-units --all | grep kodi
sudo find /etc/systemd -name "*kodi*"
sudo find /home/kodi/.config/systemd -type f 2>/dev/null
```

### 5. Блокировка скомпрометированной учетной записи
```bash
# Заблокировать пароль пользователя kodi
sudo passwd -l kodi

# Изменить shell для предотвращения входа
sudo usermod -s /sbin/nologin kodi

# Запретить SSH доступ в sshd_config
echo "DenyUsers kodi" | sudo tee -a /etc/ssh/sshd_config
sudo systemctl restart sshd
```

---

## 🔒 Безопасность роутера (MikroTik)

### Исправление открытого dst-nat правила

#### Через CLI:
```bash
# Подключиться к MikroTik
ssh admin@ip-роутера

# Список NAT правил
/ip firewall nat print

# Удалить проблемное dst-nat правило для SSH
/ip firewall nat remove [номер]
```

#### Через WinBox:
1. Открыть WinBox и подключиться к роутеру
2. Перейти в **IP → Firewall → NAT**
3. Найти правило с `dst-port=22`
4. **Удалить** это правило

### Добавление защиты от SSH brute-force
```bash
# Защита MikroTik от SSH brute-force
/ip firewall filter
add chain=input protocol=tcp dst-port=22 src-address-list=ssh_blacklist action=drop comment="Блокировка IP из черного списка SSH"
add chain=input protocol=tcp dst-port=22 connection-state=new src-address-list=ssh_stage3 action=add-src-to-address-list address-list=ssh_blacklist address-list-timeout=1w comment="Этап 3: Черный список"
add chain=input protocol=tcp dst-port=22 connection-state=new src-address-list=ssh_stage2 action=add-src-to-address-list address-list=ssh_stage3 address-list-timeout=1m comment="Этап 2"
add chain=input protocol=tcp dst-port=22 connection-state=new src-address-list=ssh_stage1 action=add-src-to-address-list address-list=ssh_stage2 address-list-timeout=1m comment="Этап 1"
add chain=input protocol=tcp dst-port=22 connection-state=new action=add-src-to-address-list address-list=ssh_stage1 address-list-timeout=1m comment="Обнаружение новых SSH подключений"
```

---

## 🔐 Усиление безопасности SSH сервера

### 1. Отключение аутентификации по паролю (только ключи)
```bash
# Редактировать SSH конфиг
sudo nano /etc/ssh/sshd_config
```

Добавить/изменить эти строки:
```
PasswordAuthentication no
PermitRootLogin no
PubkeyAuthentication yes
AllowUsers ваш_админ_пользователь  # НЕ kodi
```

### 2. Изменение SSH порта (опционально)
```bash
# В /etc/ssh/sshd_config
Port 22222  # Использовать нестандартный порт
```

### 3. Установка Fail2Ban
```bash
# Установить fail2ban
sudo apt update
sudo apt install fail2ban -y

# Включить и запустить
sudo systemctl enable fail2ban
sudo systemctl start fail2ban

# Проверить статус
sudo fail2ban-client status sshd
```

### 4. Перезапуск SSH сервиса
```bash
sudo systemctl restart sshd
```

---

## 🔬 Глубокая проверка системы

### 1. Проверка запущенных процессов
```bash
# Список всех процессов
ps aux | grep -E "kswapd00|kauditd0|edac0|configrc|init01" | grep -v grep

# Мониторинг использования CPU
top -bn1 | head -20
htop  # интерактивный режим
```

### 2. Проверка сетевых соединений
```bash
# Активные сетевые соединения
sudo netstat -tunap | grep ESTABLISHED
sudo ss -tunap | grep ESTABLISHED

# Прослушиваемые порты
sudo netstat -tulpn
sudo ss -tulpn
```

### 3. Проверка Cron задач
```bash
# Пользовательские crontab
sudo crontab -l
sudo crontab -u kodi -l
sudo crontab -u root -l

# Системный cron
sudo cat /etc/crontab
sudo ls -la /etc/cron.d/
sudo cat /var/spool/cron/crontabs/*
```

### 4. Проверка скриптов автозапуска
```bash
# rc.local (старый стиль автозапуска)
sudo cat /etc/rc.local

# Файлы профиля Bash
sudo cat /home/kodi/.bashrc | tail -20
sudo cat /home/kodi/.profile | tail -20
sudo cat /home/kodi/.bash_profile 2>/dev/null
```

### 5. Поиск оставшихся вредоносов
```bash
# Поиск по имени файла
sudo find / -name "*kswapd*" 2>/dev/null
sudo find / -name "*configrc*" 2>/dev/null
sudo find / -name "*kauditd*" 2>/dev/null

# Поиск во временных директориях
sudo find /tmp /var/tmp -type f -user kodi 2>/dev/null

# Поиск недавно измененных файлов
sudo find /home/kodi -type f -mtime -7 -ls
```

### 6. Просмотр команд атакующих
```bash
# Проверить историю bash чтобы увидеть что делали атакующие
sudo cat /home/kodi/.bash_history
sudo cat /root/.bash_history

# Примечание: Атакующие часто УДАЛЯЮТ .bash_history чтобы скрыть следы
# Если отсутствует, проверьте:
sudo ls -la /home/kodi/.bash_history
sudo lsattr /home/kodi/.bash_history 2>/dev/null

# Проверить что было установлено в точное время взлома (09:23:42)
sudo find / -type f -newermt "2026-01-31 09:23:40" ! -newermt "2026-01-31 09:24:00" -ls 2>/dev/null | grep -v "/proc\|/sys\|/run"

# Проверить активность systemd во время взлома
sudo journalctl _UID=$(id -u kodi) --since "2026-01-31 09:23:00" --until "2026-01-31 09:25:00"
```

---

## 🛡️ Обнаружение руткитов

### Установка инструментов безопасности
```bash
sudo apt update
sudo apt install rkhunter chkrootkit debsums -y
```

### Запуск сканеров руткитов
```bash
# rkhunter
sudo rkhunter --update
sudo rkhunter --check --sk

# chkrootkit
sudo chkrootkit

# Проверка целостности пакетов
sudo debsums -c | head -50
```

---

## 📊 Проверка очистки

### 1. Подтверждение отсутствия подозрительных процессов
```bash
ps aux | grep kodi
# Не должно показывать вредоносных процессов
```

### 2. Проверка отсутствия внешних соединений
```bash
sudo netstat -tunap | grep ESTABLISHED
# Должны быть только легитимные соединения
```

### 3. Проверка нормального использования CPU
```bash
top -bn1 | head -20
# CPU должен быть в норме (не 400%+)
```

### 4. Проверка что SSH не открыт извне
```bash
# Узнать ваш публичный IP
curl ifconfig.me

# С другой сети просканировать ваш IP
nmap -p 22 ВАШ_ПУБЛИЧНЫЙ_IP
# Должно показать: closed или filtered (НЕ open)
```

### 5. Проверка что Cron чист
```bash
sudo crontab -l
sudo crontab -u kodi -l  # Должно быть пусто или ошибка
```

---

## ⚠️ Важные рекомендации

### Критически важно: Переустановка системы
После взлома такого масштаба, **единственный способ быть на 100% уверенным** что система чиста это:

1. **Сделать резервную копию важных данных** (не исполняемых файлов)
2. **Переустановить операционную систему с нуля**
3. **Восстановить только файлы данных** (документы, базы данных, конфиги)
4. **Никогда не восстанавливать** исполняемые файлы, скрипты или бинарники из резервной копии

**Почему?** Атакующие могли установить:
- Руткиты на уровне ядра (если им удалось повысить привилегии)
- Модифицированные системные бинарники
- Скрытые бэкдоры
- Постоянные вредоносы, которые вы не нашли

**Важное примечание**: В данном конкретном инциденте пользователь `kodi` **НЕ ИМЕЛ sudo привилегий**, что ограничило возможности атакующих:

✅ **Атакующие МОГЛИ:**
- Запустить криптомайнер (любой пользователь может потреблять CPU)
- Добавить SSH ключи в `~/.ssh/authorized_keys`
- Создать cron задачи для пользователя
- Скачивать и запускать файлы в домашней директории
- Устанавливать сетевые соединения

❌ **Атакующие НЕ МОГЛИ (без повышения привилегий):**
- Изменять системные файлы
- Устанавливать пакеты через apt
- Создавать новых пользователей
- Устанавливать руткиты ядра
- Изменять `/etc/ssh/sshd_config`

**Однако**, всё равно нужно проверить попытки повышения привилегий:

```bash
# Проверить не в группе ли sudo пользователь kodi
groups kodi
id kodi

# Проверить sudoers
sudo cat /etc/sudoers | grep kodi
sudo cat /etc/sudoers.d/* | grep kodi 2>/dev/null

# Проверить попытки эксплуатации SUID
sudo find / -perm -4000 -type f -mtime -7 2>/dev/null
sudo find /home/kodi /tmp /var/tmp -name "*exploit*" -o -name "*privesc*" 2>/dev/null

# Проверить логи sudo
sudo grep "sudo" /var/log/auth.log | grep kodi
sudo grep "su\[" /var/log/auth.log | tail -50
```

Несмотря на ограниченные привилегии, **переустановка системы всё равно настоятельно рекомендуется** потому что:
1. Атакующие могли найти неизвестные эксплойты для повышения привилегий
2. 28 различных IP получили доступ к системе - неизвестно что каждый из них делал
3. Нельзя гарантировать что все вредоносы найдены
4. Лучше перестраховаться для любой production системы

### Если переустановка невозможна
Как минимум, примите эти меры предосторожности:

1. **Смените ВСЕ пароли** в системе
2. **Перегенерируйте все SSH ключи**
3. **Тщательно мониторьте систему** неделями
4. **Запускайте регулярные сканирования на руткиты**
5. **Проверяйте логи ежедневно** на аномалии
6. **Никогда полностью не доверяйте этой системе** для критичных операций

---

## 🎯 Лучшие практики предотвращения

### 1. Никогда не открывайте SSH напрямую в интернет
- **Используйте VPN** (WireGuard, OpenVPN, L2TP) для удаленного доступа
- **Используйте SSH bastion/jump хост** если необходимо открыть SSH
- **Используйте port knocking** как дополнительный уровень безопасности
- **Никогда не используйте dst-nat** для проброса порта 22 из WAN

### 2. Усиление SSH
```bash
# Лучшие практики для /etc/ssh/sshd_config
PermitRootLogin no
PasswordAuthentication no  # Только ключи
PubkeyAuthentication yes
Port 22222  # Нестандартный порт
AllowUsers admin_user  # Белый список конкретных пользователей
MaxAuthTries 3
LoginGraceTime 30
```

### 3. Использование сильной аутентификации
- **Только SSH ключи** (4096-бит RSA или Ed25519)
- **Длинные, случайные пароли** если пароли необходимы
- **Двухфакторная аутентификация** (Google Authenticator)

### 4. Установка инструментов безопасности
```bash
# Обнаружение вторжений
sudo apt install fail2ban

# Firewall
sudo apt install ufw
sudo ufw enable
sudo ufw allow from ВАШ_IP to any port 22

# Мониторинг логов
sudo apt install logwatch
```

### 5. Регулярные проверки безопасности
```bash
# Еженедельные проверки
sudo rkhunter --check
sudo chkrootkit
sudo debsums -c

# Мониторинг логов
sudo tail -f /var/log/auth.log
sudo grep "Failed password" /var/log/auth.log | tail -50
```

### 6. Сегментация сети
- **Отдельная управляющая сеть** от публичных сервисов
- **VLAN изоляция** для критических систем
- **Правила firewall** между сегментами сети

---

## 📝 Хронология атаки (пример из инцидента)

```
31 янв 01:31:59 - Первый успешный вход с 91.215.85.88 (Нидерланды)
31 янв 01:33:46 - Вход с 185.244.183.190 (Германия)
31 янв 01:33:52 - Вход с 185.173.39.145 (Германия)
31 янв 01:54:54 - Вход с 123.156.230.101 (Китай)
31 янв 09:23:42 - КРИТИЧЕСКИ: Вход с 194.163.178.53 (Россия) - УСТАНОВКА ВРЕДОНОСА
31 янв 09:23:42 - Открыта сессия, запущены процессы systemd
31 янв 14:13:00 - Запущены процессы вредоноса
31 янв 14:14:00 - kauditd0 потребляет 546% CPU (криптомайнер)
31 янв 14:38:00 - Продолжаются массовые brute-force попытки с множественных IP
```

**Время до компрометации**: Менее 8 часов после открытия порта

---

## 🚨 Масштаб атаки ботнета (данные реального инцидента)

### Статистика атаки:
- **28 уникальных IP** успешно вошли в систему
- **Сотни неудачных попыток** от множества других IP
- **География**: Россия, Германия, Нидерланды, Китай
- **Время взлома**: Менее 8 часов после открытия SSH порта
- **Финальная компрометация**: 31 января 2026 в 09:23:42 UTC
- **Пароль**: Слабый пароль (вероятно "kodi" или "1234") взломан ботнетом

### IP адреса ботнета (успешные входы):

```
185.173.37.38      185.173.37.63      185.173.37.83      185.173.38.141
185.173.39.145     185.173.39.170     185.244.180.175    185.244.183.107
185.244.183.190    194.163.178.53 ← УСТАНОВКА ВРЕДОНОСА
77.232.36.176      77.232.37.22       77.232.38.102      77.232.38.170
77.232.38.211      77.232.41.132      77.232.42.240      77.232.43.15
77.232.43.76       91.142.77.101      91.142.77.37       91.142.77.63
91.142.78.22       91.142.78.60       91.142.78.8        91.215.85.88
123.156.230.101    176.120.22.13
```

### Распределение по IP диапазонам:
- **185.173.x.x** (7 IP) - Германия (хостинг)
- **185.244.x.x** (3 IP) - Германия (хостинг)  
- **77.232.x.x** (10 IP) - Россия (хостинг)
- **91.142.x.x** (6 IP) - Россия (хостинг)
- **91.215.85.88** - Нидерланды
- **194.163.178.53** - Россия (финальный атакующий, установивший вредонос)
- **123.156.230.101** - Китай
- **176.120.22.13** - Россия

### Как проверить вашу систему на активность ботнета:

```bash
# Подсчитать уникальные IP успешно вошедшие в систему
sudo grep "Accepted password for kodi" /var/log/auth.log | awk '{print $11}' | sort -u | wc -l

# Список всех IP
sudo grep "Accepted password for kodi" /var/log/auth.log | awk '{print $11}' | sort -u

# Проверить неудачные попытки (обычно намного больше)
sudo grep "Failed password for kodi" /var/log/auth.log | wc -l
```

### Блокировка IP ботнета на роутере MikroTik:

```bash
# MikroTik - Заблокировать диапазоны IP атакующих
/ip firewall address-list
add list=blacklist address=185.173.0.0/16 comment="Ботнет Германия"
add list=blacklist address=185.244.0.0/16 comment="Ботнет Германия"
add list=blacklist address=77.232.0.0/16 comment="Ботнет Россия"
add list=blacklist address=91.142.0.0/16 comment="Ботнет Россия"
add list=blacklist address=91.215.0.0/16 comment="Ботнет Нидерланды"
add list=blacklist address=194.163.0.0/16 comment="Ботнет Россия"
add list=blacklist address=123.156.0.0/16 comment="Ботнет Китай"

# Применить правила блокировки
/ip firewall filter
add chain=input src-address-list=blacklist action=drop comment="Блокировка IP ботнета"
add chain=forward src-address-list=blacklist action=drop comment="Блокировка пересылки ботнета"
```

### Критические выводы:

**Это была НЕ целевая атака** - это был автоматический ботнет, сканирующий весь интернет в поисках открытых SSH серверов со слабыми паролями.

**Ключевые моменты:**
1. ✅ Пароль был крайне слабым - взломан за несколько часов
2. ⚠️ **28 различных IP** из координированного ботнета получили доступ
3. 🚨 Финальный атакующий (194.163.178.53) установил полный набор вредоносов
4. 💀 Система была **полностью скомпрометирована** распределённым ботнетом
5. ⚡ **ПЕРЕУСТАНОВКА СИСТЕМЫ ОБЯЗАТЕЛЬНА** - другого безопасного варианта нет

**Вы не можете доверять этой системе** после того как 28 IP из ботнета имели к ней доступ!

---

## 🔗 Полезные ресурсы

- [SSH лучшие практики - Mozilla](https://infosec.mozilla.org/guidelines/openssh)
- [CIS Benchmark для Debian](https://www.cisecurity.org/benchmark/debian_linux)
- [Документация fail2ban](https://www.fail2ban.org/)
- [Документация rkhunter](http://rkhunter.sourceforge.net/)
- [Безопасность MikroTik](https://wiki.mikrotik.com/wiki/Manual:Securing_Your_Router)

---

## 📧 Контакты и вклад

Если вы столкнулись с похожим взломом или у вас есть дополнительные шаги по устранению последствий, пожалуйста, внесите свой вклад!

**Помните**: Безопасность - это процесс, а не конечная цель. Будьте бдительны!

---

## ⚖️ Лицензия

Этот документ предоставляется в образовательных целях. Используйте на свой риск.

**Отказ от ответственности**: Всегда консультируйтесь со специалистами по безопасности для production систем.

---

## ✅ Финальная проверка после очистки

После выполнения всех шагов по устранению последствий взлома, выполните финальную проверку:

### 1. Проверка процессов
```bash
# Убедиться что нет процессов от kodi
ps aux | grep kodi | grep -v grep
# Результат должен быть пустым

# Проверка CPU нагрузки (должна быть нормальной)
uptime
top -bn1 | head -10
```

### 2. Проверка механизмов постоянства
```bash
# Cron задачи kodi (должно быть пусто или ошибка)
sudo crontab -u kodi -l

# SSH ключи удалены
sudo ls -la /home/kodi/.ssh/

# Пользователь заблокирован
id kodi
# Должно показать shell: /sbin/nologin
```

### 3. Проверка сети
```bash
# SSH не открыт в интернет (проверить с внешнего компьютера)
# С другой сети:
nmap -p 22 ВАШ_ВНЕШНИЙ_IP
# Должно быть: closed или filtered

# Проверка открытых портов
sudo ss -tulpn | grep ssh
```

### 4. Проверка MikroTik
В настройках роутера убедитесь:
- ✅ dst-nat правило для порта 22 **удалено**
- ✅ DMZ **отключен**
- ✅ Firewall правила для блокировки ботнета **добавлены**

### 5. Сканирование на вредоносы
```bash
# Обновить локальные базы
sudo rkhunter --propupd

# Запустить проверку
sudo rkhunter --check --skip-keypress --report-warnings-only

# Альтернативный сканер
sudo chkrootkit | grep -i infected
```

### 6. Проверка Debian репозиториев
После устранения инцидента обновите систему:
```bash
# Исправить sources.list для Debian 10 (в архиве)
sudo tee /etc/apt/sources.list > /dev/null <<'EOF'
deb http://archive.debian.org/debian/ buster main contrib non-free
deb http://archive.debian.org/debian-security/ buster/updates main contrib non-free
EOF

# Отключить проверку Valid-Until
sudo tee /etc/apt/apt.conf.d/99no-check-valid-until > /dev/null <<'EOF'
Acquire::Check-Valid-Until "false";
EOF

# Обновить
sudo apt update
sudo apt upgrade
```

---

## 🎓 Выводы и уроки

### Что пошло не так:
1. ❌ SSH порт 22 был открыт в интернет через dst-nat на MikroTik
2. ❌ Слабый пароль пользователя `kodi` (вероятно "kodi" или "1234")
3. ❌ Не был установлен fail2ban для защиты от brute-force
4. ❌ Не использовалась аутентификация только по SSH ключам

### Масштаб ущерба:
- **28 уникальных IP** из ботнета получили доступ
- Установлен криптомайнер, потребляющий **546% CPU**
- Добавлены SSH ключи для постоянного доступа
- Созданы cron задачи для автозапуска
- История команд `.bash_history` удалена атакующими

### Положительные моменты:
- ✅ Пользователь `kodi` **не имел sudo привилегий**
- ✅ Атакующие **не получили root доступ**
- ✅ Системные файлы скорее всего **не изменены**
- ✅ Классические руткиты **не обнаружены**
- ✅ Все вредоносные процессы успешно **остановлены**

### Критические действия выполнены:
- ✅ Вредоносные файлы из `/home/kodi/.configrc7/` удалены
- ✅ SSH ключи атакующих удалены
- ✅ Cron задачи очищены
- ✅ Пользователь `kodi` заблокирован
- ✅ dst-nat правило на MikroTik удалено
- ✅ SSH порт больше не доступен из интернета

---

## 🔮 Что дальше?

### Краткосрочные действия (уже выполнено):
1. ✅ Остановка вредоносных процессов
2. ✅ Удаление вредоносных файлов
3. ✅ Блокировка скомпрометированного пользователя
4. ✅ Закрытие SSH из интернета
5. ✅ Сканирование на руткиты

### Среднесрочные действия (рекомендуется):
1. **Мониторинг системы** в течение 2-4 недель:
   - Ежедневная проверка логов: `sudo tail -100 /var/log/auth.log`
   - Еженедельное сканирование: `sudo rkhunter --check`
   - Проверка процессов: `top`, `ps aux`
   - Мониторинг CPU: `uptime`

2. **Смена всех паролей**:
   ```bash
   sudo passwd root
   sudo passwd your_admin_user
   # Генерировать сложные пароли: openssl rand -base64 24
   ```

3. **Обновление до актуальной Debian**:
   - Debian 10 (Buster) больше не поддерживается
   - Рекомендуется обновиться до Debian 11 (Bullseye) или Debian 12 (Bookworm)

### Долгосрочные действия (обязательно):

**⚠️ КРИТИЧЕСКИ ВАЖНО: ПЕРЕУСТАНОВКА СИСТЕМЫ**

Несмотря на успешную очистку, **настоятельно рекомендуется полная переустановка** по следующим причинам:

1. **28 различных IP** из ботнета имели доступ к системе
2. Неизвестно что каждый из них делал
3. Возможны неизвестные эксплойты для повышения привилегий
4. Невозможно гарантировать отсутствие скрытых бэкдоров
5. Для production систем - единственный безопасный вариант

**План переустановки:**
1. Резервное копирование данных (БЕЗ исполняемых файлов)
2. Установка Debian 12 (Bookworm) с нуля
3. Настройка безопасности с самого начала
4. Восстановление только файлов данных

---

## 📋 Чеклист безопасности для новой установки

После переустановки или на другом сервере используйте этот чеклист:

### SSH безопасность:
- [ ] PasswordAuthentication no (только ключи)
- [ ] PermitRootLogin no
- [ ] Нестандартный порт (не 22)
- [ ] AllowUsers (белый список пользователей)
- [ ] fail2ban установлен и настроен

### Firewall:
- [ ] ufw или iptables настроен
- [ ] SSH доступен только из белого списка IP
- [ ] Все ненужные порты закрыты

### Роутер MikroTik:
- [ ] Никакого dst-nat для SSH
- [ ] DMZ отключен
- [ ] VPN настроен для удалённого доступа
- [ ] Брандмауэр защищает от brute-force

### Мониторинг:
- [ ] logwatch установлен
- [ ] rkhunter еженедельные проверки
- [ ] Регулярный просмотр /var/log/auth.log

### Обновления:
- [ ] unattended-upgrades настроен
- [ ] Актуальная версия Debian (11 или 12)
- [ ] Регулярный apt update && apt upgrade

---

## 🆘 Контакты для помощи

Если вы столкнулись с похожей ситуацией:

1. **Не паникуйте** - следуйте этому руководству шаг за шагом
2. **Отключите сервер от интернета** - первым делом
3. **Задокументируйте всё** - логи, процессы, файлы
4. **Обратитесь к специалистам** если не уверены

### Полезные ресурсы:
- [Debian Security](https://www.debian.org/security/)
- [MikroTik Security Guide](https://wiki.mikrotik.com/wiki/Manual:Securing_Your_Router)
- [SSH Hardening](https://infosec.mozilla.org/guidelines/openssh)
- [fail2ban Documentation](https://www.fail2ban.org/)

---

## 💡 Финальное напоминание

**Безопасность - это процесс, а не конечная точка.**

Основные принципы:
1. **Минимальная поверхность атаки** - закрывайте всё ненужное
2. **Глубокая оборона** - несколько уровней защиты
3. **Принцип наименьших привилегий** - не давайте лишних прав
4. **Регулярный мониторинг** - проверяйте логи и систему
5. **Быстрое реагирование** - чем раньше обнаружили, тем меньше ущерб

**Помните**: Взлом произошёл менее чем за 8 часов после открытия порта. Автоматические боты сканируют весь интернет 24/7. Будьте бдительны!

---

**Этот инцидент завершён успешно. Система очищена, защищена и задокументирована.**

**Дата инцидента**: 31 января 2026  
**Время обнаружения**: ~14:14 UTC  
**Время устранения**: ~16:30 UTC  
**Статус**: ✅ Угроза нейтрализована, рекомендуется переустановка
